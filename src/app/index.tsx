import RouteConfig from '@config/route.config';

import { HashRouter as Router } from 'react-router-dom';

function App() {
  return (
    <Router>
      <RouteConfig />
    </Router>
  );
}

export default App;
